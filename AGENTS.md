# 知模 (ZhiMoHub) 团队协作与技术规范 (AGENTS.md)

> 本文件是本项目的最高级别指引与协作行为准则。任何 AI 助手在此项目中协作时，必须严格遵守。

---

## 🎯 核心原则：开发即沉淀，精益求精

1. **真实性原则**：
   - 严禁在业务库中塞入虚构的假数据或未经验证的假文件。
   - 所有 SketchUp 资产的解析、缩略图提取、文件落盘必须真实有效，确保可正常在 3D 软件中打开使用。

2. **技术栈与架构规范**：
   - **后端**：Python 3.11 + FastAPI + SQLModel (SQLite 零运维单文件数据库) + 纯 Python 二进制流无头解析。
   - **前端**：React 19 + Vite + TailwindCSS v4 + TypeScript，严格遵守类型安全与简洁架构。
   - **统一托管**：保持 React 前端打包产物由 FastAPI 静态托管的单端口运行模式，方便私有化部署。

3. **代码与文档规范**：
   - 遵循最小修改原则，不随意变更与当前任务无关的已有逻辑。
   - 所有 Git Commit 信息使用中文简体编写，遵循 `<type>: <subject>` 规范。

---

## 🗺️ 核心业务与概念速查表

| 概念 / 模块 | 对应技术实现 | 通俗说明 |
| :--- | :--- | :--- |
| **SKP 二进制流** | `skp_parser.py` (纯 Python) | 免装客户端直接解构 `.skp` 专有文件头与 ZIP 容器。 |
| **版本提取** | UTF-16LE 正则匹配 `{主版本.次版本}` | 精确识别 SketchUp 2018 ~ 2026（例如 `{26.2.242}` = SU 2026）。 |
| **缩略图抽取** | 二进制 PNG 魔数 / ZIP 抽取 | 毫秒级直接抠出设计师保存视口的原生高清预览图。 |
| **自动分类** | `classifier.py` 规则库 | 依据文件名与构件自动归入园林植物、街景设施、公园小品、景观构件等风景园林专业品类。 |
| **相似模型推荐**| 三维长宽深比 + 构件语义重合度 | 按空间几何比例与形态，智能聚类库内相似款式。 |
| **单端口托管** | FastAPI `StaticFiles` + SPA 兜底 | 一个端口（8000）搞定前后端全部请求与静态页面访问。 |

---

## 📋 本项目四阶段连载教程与研发路线

- [x] **阶段 01**：零客户端依赖：纯 Python 解构 SketchUp 二进制文件与秒级缩略图提取
- [x] **阶段 02**：数据落地与分类：基于 SQLModel + SQLite 打造私有化 3D 资产库与智能自动打标
- [x] **阶段 03**：空间特征计算：三维包围盒比例算法与相似模型一键聚类推荐
- [ ] **阶段 04**：多模态赋能：集成 CLIP / Embedding 向量库实现自然语言意图搜索与以图搜 3D 模型

